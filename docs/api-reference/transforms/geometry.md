<!-- Generated by tools/generate_api_docs.py. Do not edit directly. -->

# Geometry transforms

Transforms that change an image grid, extent, orientation, or resolution.

## ToImage

```text
class imzdesk.transforms.ToImage(
    target_mpp=None,
    shape=None,
    crop=True,
    interpolation=None,
)
```

Initialize conversion from image-like objects to dense NumPy arrays.

**Parameters**

- `target_mpp` (`float or tuple of float`): Target microns per pixel for objects
  that support physical image scaling. A scalar applies to both axes.
- `shape` (`tuple of int`): Optional `(height, width)` output shape for
  rasterized data containers.
- `crop` (`bool`): Whether image file classes should apply their metadata crop
  when one is available.
- `interpolation` (`{None, 'nearest'}`): Optional interpolation for dense data
  containers. `'nearest'` fills unmeasured raster locations from the nearest
  measured pixel.

### __call__

```text
imzdesk.transforms.ToImage.__call__(image)
```

Convert an image-like object to a dense array.

**Parameters**

- `image` (`DImage or WSI`): Image-like value to rasterize.

**Returns**

- `numpy.ndarray`: Rasterized image.

## CenterCrop

```text
class imzdesk.transforms.CenterCrop(size, mpp=None)
```

Initialize a centered spatial crop.

**Parameters**

- `size` (`int or tuple of int`): Output `(height, width)` in pixels.
- `mpp` (`float or tuple of float`): Output microns per pixel.

## Pad

```text
class imzdesk.transforms.Pad(padding, fill=0)
```

Initialize padding for an in-memory image.

**Parameters**

- `padding` (`int or sequence of int`): Uniform padding, horizontal/vertical
  padding, or `(left, top, right, bottom)` padding.
- `fill` (`scalar or sequence`): Fill value for raster data.

### __call__

```text
imzdesk.transforms.Pad.__call__(image)
```

Pad one image or both members of a pair.

## RandomAffine

```text
class imzdesk.transforms.RandomAffine(
    degrees,
    translate=None,
    scale=None,
    shear=None,
    interpolation='nearest',
    fill=0,
    seed=None,
)
```

Initialize random affine augmentation.

**Parameters**

- `degrees` (`float or tuple of float`): Rotation range in degrees.
- `translate` (`tuple of float`): Maximum horizontal and vertical translation
  fractions.
- `scale` (`float or tuple of float`): Isotropic scale range.
- `shear` (`float or sequence of float`): Horizontal and optional vertical shear
  ranges in degrees.
- `interpolation` (`{'nearest', 'bilinear', 'bicubic'}`): Raster interpolation
  mode.
- `fill` (`scalar or sequence`): Fill value outside the source extent.
- `seed` (`int`): Base random seed.

### __call__

```text
imzdesk.transforms.RandomAffine.__call__(image)
```

Apply a sampled affine transform to one image or a pair.

## RandomHorizontalFlip

```text
class imzdesk.transforms.RandomHorizontalFlip(p=0.5, seed=None)
```

Initialize random horizontal flipping.

**Parameters**

- `p` (`float`): Probability of flipping a sample.
- `seed` (`int`): Base random seed.

### __call__

```text
imzdesk.transforms.RandomHorizontalFlip.__call__(image)
```

Randomly flip one image or both members of a pair horizontally.

## RandomResizedCrop

```text
class imzdesk.transforms.RandomResizedCrop(
    size,
    scale=(0.08, 1.0),
    ratio=(3 / 4, 4 / 3),
    seed=None,
)
```

Initialize random crop-and-resize augmentation.

**Parameters**

- `size` (`int or sequence of int`): Output `(height, width)`.
- `scale` (`tuple of float`): Range of overlap-area fractions to crop.
- `ratio` (`tuple of float`): Range of crop aspect ratios.
- `seed` (`int`): Base random seed.

### __call__

```text
imzdesk.transforms.RandomResizedCrop.__call__(image)
```

Crop a random physical field and rasterize it to a fixed size.

## RandomRotate90

```text
class imzdesk.transforms.RandomRotate90(p=0.5, choices=(1, 2, 3), seed=None)
```

Initialize random right-angle rotation.

**Parameters**

- `p` (`float`): Probability of rotating a sample.
- `choices` (`sequence of int`): Candidate numbers of quarter turns.
- `seed` (`int`): Base random seed.

### __call__

```text
imzdesk.transforms.RandomRotate90.__call__(image)
```

Rotate a sample by a randomly selected multiple of 90 degrees.

## RandomVerticalFlip

```text
class imzdesk.transforms.RandomVerticalFlip(p=0.5, seed=None)
```

Initialize random vertical flipping.

**Parameters**

- `p` (`float`): Probability of flipping a sample.
- `seed` (`int`): Base random seed.

### __call__

```text
imzdesk.transforms.RandomVerticalFlip.__call__(image)
```

Randomly flip one image or both members of a pair vertically.

## Resample

```text
class imzdesk.transforms.Resample(mpp, interpolation='bilinear')
```

Initialize resampling at a target physical resolution.

**Parameters**

- `mpp` (`float or sequence of float`): Target microns per pixel.
- `interpolation` (`{'nearest', 'bilinear', 'bicubic'}`): Raster interpolation
  mode.

### __call__

```text
imzdesk.transforms.Resample.__call__(image)
```

Resample an image while preserving its physical field of view.

## Resize

```text
class imzdesk.transforms.Resize(size, interpolation='bilinear')
```

Initialize image-grid resizing.

**Parameters**

- `size` (`int or sequence of int`): Output `(height, width)`.
- `interpolation` (`{'nearest', 'bilinear', 'bicubic'}`): Raster interpolation
  mode.

### __call__

```text
imzdesk.transforms.Resize.__call__(image)
```

Resize one image or both members of a pair.

## RandomCrop

```text
class imzdesk.transforms.RandomCrop(size, mpp=None, seed=None)
```

Initialize random spatial cropping.

**Parameters**

- `size` (`int or tuple of int`): Output `(height, width)` in pixels.
- `mpp` (`float or tuple of float`): Output microns per pixel.
- `seed` (`int`): Base random seed.

### __call__

```text
imzdesk.transforms.RandomCrop.__call__(image)
```

Randomly crop one image or a registered pair in a shared frame.
